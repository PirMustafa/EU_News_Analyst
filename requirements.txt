# --- Core App Framework ---
streamlit>=1.61.1

# --- Embeddings & ML Runtime ---
sentence-transformers>=3.0.0
# torch comes in via sentence-transformers. The extra index below makes pip prefer
# the CPU-only build: PEP 440 sorts the local version `2.x.y+cpu` above plain
# `2.x.y`, so this avoids the ~2.5 GB CUDA wheel that breaks Streamlit Cloud.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# --- Vector Database & Math ---
faiss-cpu>=1.7.4
numpy>=1.24.0

# --- Web Scraping & Data Ingestion ---
requests>=2.34.2
beautifulsoup4>=4.12.0

# --- Voice & Audio Processing ---
SpeechRecognition>=3.10.0
edge-tts>=6.1.0

# --- Development Utilities ---
tqdm>=4.66.0
